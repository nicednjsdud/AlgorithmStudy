import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Back_3035 {
    public static void main(String[] args) throws IOException {

        // given
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        StringTokenizer token = new StringTokenizer(br.readLine());
        int row = Integer.parseInt(token.nextToken());      // 행입력
        int column = Integer.parseInt(token.nextToken());   // 열입력
        int ZR = Integer.parseInt(token.nextToken());       // 행으로 늘릴 배수
        int ZC = Integer.parseInt(token.nextToken());       // 열로 늘릴 배수
        // when
        for(int i=0;i<row;i++){
            for(int j=0;j<column;j++){
                token= new StringTokenizer(br.readLine());
                
            }
        }

        // then
    }
}
